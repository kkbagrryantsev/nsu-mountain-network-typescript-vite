export interface UseRecord {
    id: string
    isConfirmed: boolean
    itemId: string
    quantity: number
    userId: string
    warehousemanId: string
    description: string
    startDate: Date
    endDate: Date
}